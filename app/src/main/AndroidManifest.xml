<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.promtnow_rot">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Reimbursement of Traveling"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".profile.ProfileActivity"></activity>
        <activity android:name=".reject.RejectActivity" />
        <activity android:name=".pending.PendingActivity" />
        <activity android:name=".approve.ApproveActivity" />
        <activity android:name=".add.AddActivity" >

        </activity>
        <activity android:name=".setting.SettingActivity" />
        <activity android:name=".homepage.MainPageActivity">

        </activity>
        <activity android:name=".LauncherActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".register.RegisterActivity" />
        <activity
            android:name=".LoginActivity"
            android:windowSoftInputMode="adjustPan" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>